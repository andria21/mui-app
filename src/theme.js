import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#5ECE7B",
    },
    background: {
      default: "#fff",
    },
  },
});

export default theme;

// #764abc